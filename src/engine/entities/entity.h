#ifndef ENTITY_H
#define ENTITY_H

#include <ostream>
#include "engine/util/common_includes.h"

class Entity
{
    public:
        Entity();
        virtual ~Entity();
    protected:

    private:
};

#endif // ENTITY_H
